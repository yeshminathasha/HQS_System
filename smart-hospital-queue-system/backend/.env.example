# Spring Boot backend environment variables.
# Set these as real environment variables when running the backend
# (Spring Boot does not read this file; `docker --env-file`, your shell,
# or your IDE run configuration must export them).
PORT=8080
MONGODB_URI=mongodb://localhost:27017/smarthospital
CORS_ALLOWED_ORIGINS=http://localhost:3000
